{% extends 'base.html.twig' %}

{% block title %}Hello RaceController!{% endblock %}

{% block body %}

    <div class="d-flex align-items-center justify-content-center" style="height: 35px">
        <h3>wyścigi: </h3>
    </div>

    <div class="d-flex align-items-center justify-content-center">
        <table class="table table-striped">
            <thead>
            <tr>
                <th scope="col">data</th>
                <th scope="col">nazwa</th>
            </tr>
            </thead>
            <tbody>
            {% for race in races %}
                <tr>
                    <th scope="row">{{ race.getDate()|date("d-m-Y") }}</th>
                    <td><a style="" href="{{ path('app_show_one_race',{'id':race.getId()}) }}">{{ race.getName() }}</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
