{% extends 'base.html.twig' %}

{% block title %}Hello RaceController!{% endblock %}

{% block body %}


    <div class="d-flex align-items-center justify-content-center" style="height: 50px;margin-top: 50px">
        <h3>Wybierz rajdowców!</h3>
    </div>
    <div class="d-flex align-items-center justify-content-center" style="height: 200px">
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.driversIds) }}
        {{ form_row(form.send) }}
        {{ form_end(form) }}
    </div>

{% endblock %}
