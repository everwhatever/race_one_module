{% extends 'base.html.twig' %}

{% block title %}Hello RaceController!{% endblock %}

{% block body %}

    <div class="d-flex align-items-center justify-content-center" style="height: 350px">
        <h1>Wybierz rajdowców!</h1>
    </div>
    <div class="d-flex align-items-center justify-content-center" style="height: 350px">
        {{ form_start(form) }}
        {{ form_row(form.email) }}
        {{ form_row(form.send) }}
        {{ form_end(form) }}
    </div>

{% endblock %}
